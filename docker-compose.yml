services:
  app:
    image: openhire-web:latest
    container_name: openhire
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
